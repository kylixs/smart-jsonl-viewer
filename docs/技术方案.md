# JSONL Viewer Chrome 扩展 - 实施方案

## 一、项目概述

### 核心功能
1. 展开/折叠 JSON Lines 和 JSON 层次结构
2. 智能递归解码复杂字符串（`\n`、`\"`等转义字符）
3. 支持查找过滤（原始内容 + 解码后内容）
4. 两种过滤模式：按行过滤 / 按叶子节点过滤

### 使用场景
- **场景 A**：打开本地 `.jsonl` 文件查看
- **场景 B**：自动美化网页中的 JSONL API 响应（类似 JSONView）

---

## 二、技术架构

### 技术栈
- **框架**：Vue 3 + TypeScript
- **构建工具**：Vite
- **UI 组件库**：无（使用原生 CSS，保持轻量）
- **状态管理**：Pinia（轻量级）
- **虚拟列表**：vue-virtual-scroller（可选，如需后续支持大文件）

### Chrome 扩展架构

```
chrome-extension/
├── manifest.json              # V3 清单文件
├── src/
│   ├── content/               # 内容脚本（拦截网页 JSONL）
│   │   └── content.ts
│   ├── viewer/                # 查看器主界面
│   │   ├── App.vue
│   │   ├── main.ts
│   │   ├── components/
│   │   │   ├── JsonLineItem.vue      # 单行 JSON 组件
│   │   │   ├── JsonTree.vue          # JSON 树形结构
│   │   │   ├── StringDecoder.vue     # 字符串解码器
│   │   │   └── SearchFilter.vue      # 搜索过滤器
│   │   ├── stores/
│   │   │   └── jsonlStore.ts         # 数据状态管理
│   │   └── utils/
│   │       ├── decoder.ts            # 递归解码逻辑
│   │       ├── filter.ts             # 过滤逻辑
│   │       └── parser.ts             # JSONL 解析
│   ├── background/            # 后台脚本
│   │   └── background.ts
│   └── popup/                 # 弹出窗口（可选）
│       └── popup.vue
├── public/
│   ├── icons/                 # 扩展图标
│   └── viewer.html            # 查看器 HTML 模板
└── vite.config.ts
```

---

## 三、核心功能设计

### 3.1 展开/折叠机制

#### 数据结构
```typescript
interface JsonLineNode {
  id: string;                    // 唯一标识
  lineNumber: number;            // 原始行号
  rawContent: string;            // 原始 JSON 字符串
  parsedData: any;               // 解析后的对象
  isExpanded: boolean;           // 展开状态
  decodedStrings: Map<string, DecodedString>; // 解码后的字符串缓存
}

interface JsonNode {
  key: string;
  value: any;
  type: 'object' | 'array' | 'string' | 'number' | 'boolean' | 'null';
  isExpanded: boolean;
  children?: JsonNode[];
  depth: number;
  isDecodable: boolean;          // 是否为可解码的 JSON 字符串
  decodedValue?: any;            // 解码后的值
}
```

#### 实现方式
- **行级折叠**：点击行号或箭头图标切换整行的展开状态
- **节点折叠**：JSON 树中每个对象/数组节点可独立折叠
- **递归渲染**：使用递归组件 `<JsonTree>` 渲染嵌套结构

### 3.2 智能字符串解码

#### 解码策略
```typescript
// utils/decoder.ts
export function smartDecode(value: any): DecodedResult {
  if (typeof value !== 'string') {
    return { type: 'primitive', value };
  }

  // 检测是否为 JSON 字符串
  if (isJsonString(value)) {
    try {
      const parsed = JSON.parse(value);
      // 递归解码嵌套的 JSON 字符串
      return {
        type: 'json',
        original: value,
        decoded: smartDecode(parsed),
        displayMode: 'decoded' // 或 'original'
      };
    } catch (e) {
      return { type: 'string', value };
    }
  }

  // 处理普通转义字符串
  return {
    type: 'string',
    value,
    displayValue: unescapeString(value) // 处理 \n, \", \t 等
  };
}

function isJsonString(str: string): boolean {
  const trimmed = str.trim();
  return (trimmed.startsWith('{') && trimmed.endsWith('}')) ||
         (trimmed.startsWith('[') && trimmed.endsWith(']'));
}
```

#### 显示模式
- **自动解码模式**（默认）：自动显示解码后的内容
- **原始模式**：显示原始转义字符串
- **切换按钮**：每个可解码字符串旁边有 "Raw/Decoded" 切换按钮

### 3.3 查找过滤功能

#### 过滤模式
1. **行模式**：只显示包含关键字的整行
2. **节点模式**：只显示包含关键字的叶子节点（及其路径）

#### 实现逻辑
```typescript
// utils/filter.ts
export function filterJsonLines(
  lines: JsonLineNode[],
  keyword: string,
  mode: 'line' | 'node',
  searchDecoded: boolean = true
): JsonLineNode[] {
  if (mode === 'line') {
    return lines.filter(line =>
      matchInLine(line, keyword, searchDecoded)
    );
  } else {
    return lines.map(line => ({
      ...line,
      parsedData: filterNodes(line.parsedData, keyword, searchDecoded)
    })).filter(line => line.parsedData !== null);
  }
}

function matchInLine(line: JsonLineNode, keyword: string, searchDecoded: boolean): boolean {
  // 搜索原始内容
  if (line.rawContent.includes(keyword)) return true;

  // 搜索解码后的内容
  if (searchDecoded) {
    return searchInDecodedContent(line.parsedData, keyword);
  }

  return false;
}

function filterNodes(node: any, keyword: string, searchDecoded: boolean): any {
  // 递归过滤，只保留匹配的叶子节点及其路径
  // 实现细节略...
}
```

#### UI 设计
```
┌─ 搜索栏 ─────────────────────────┐
│ [🔍] 输入关键字...               │
│ ○ 按行过滤  ● 按节点过滤        │
│ ☑ 包含解码后的内容              │
└──────────────────────────────────┘
```

### 3.4 Chrome 扩展集成

#### 场景 A：本地文件查看
```typescript
// viewer/App.vue
const handleFileUpload = async (file: File) => {
  const text = await file.text();
  const lines = text.split('\n').filter(line => line.trim());

  store.loadJsonLines(lines.map((line, index) => ({
    id: `line-${index}`,
    lineNumber: index + 1,
    rawContent: line,
    parsedData: JSON.parse(line),
    isExpanded: false,
    decodedStrings: new Map()
  })));
};
```

- 在 `viewer.html` 添加文件上传/拖拽区域
- 支持 `.jsonl`、`.json`（每行一个对象）、`.ndjson` 格式

#### 场景 B：网页 JSONL 拦截
```typescript
// content/content.ts
const detectJsonl = (text: string): boolean => {
  const lines = text.trim().split('\n');
  if (lines.length < 2) return false;

  return lines.every(line => {
    try {
      JSON.parse(line);
      return true;
    } catch {
      return false;
    }
  });
};

// 拦截网页内容
if (document.contentType === 'application/json' ||
    document.contentType === 'text/plain') {
  const bodyText = document.body.innerText;

  if (detectJsonl(bodyText)) {
    // 替换页面为查看器
    chrome.runtime.sendMessage({
      type: 'RENDER_JSONL',
      data: bodyText
    });
  }
}
```

```json
// manifest.json
{
  "manifest_version": 3,
  "name": "JSONL Viewer",
  "version": "1.0.0",
  "description": "智能 JSONL 查看器，支持字符串解码和高级过滤",
  "permissions": ["storage"],
  "content_scripts": [{
    "matches": ["<all_urls>"],
    "js": ["content.js"],
    "run_at": "document_end"
  }],
  "web_accessible_resources": [{
    "resources": ["viewer.html"],
    "matches": ["<all_urls>"]
  }]
}
```

---

## 四、UI/UX 设计

### 视觉风格
- **主题**：支持亮色/暗色主题切换
- **配色**：参考 VSCode JSON 语法高亮
  - 对象键：淡蓝色 `#9CDCFE`
  - 字符串：橙色 `#CE9178`
  - 数字：绿色 `#B5CEA8`
  - 布尔值：紫色 `#569CD6`
  - 解码标记：黄色背景 `#FFF3CD`

### 交互设计
1. **行号区域**：显示原始行号，可点击折叠
2. **JSON 树区域**：使用缩进显示层级关系
3. **工具栏**：
   - 搜索框
   - 过滤模式切换
   - 主题切换
   - 全部展开/折叠按钮
   - 导出功能（导出过滤后的数据）

### 示例界面

```
┌─ JSONL Viewer ────────────────────────────────────────┐
│ [📂 打开文件] [🔍 搜索] [⚙️ 设置] [🌙 主题]          │
├───────────────────────────────────────────────────────┤
│ 搜索: [____________] ○行模式 ●节点模式 ☑解码内容   │
├───────────────────────────────────────────────────────┤
│ 1  ▼ { type: "file-history-snapshot", ...           │
│       ├─ type: "file-history-snapshot"               │
│       ├─ messageId: "0050ba14-338f-..."             │
│       └─ snapshot: ▶ {...}                           │
│                                                       │
│ 2  ▼ { parentUuid: null, ...                        │
│       ├─ message: ▼                                  │
│       │   ├─ role: "user"                            │
│       │   └─ content: "Caveat: The messages..."     │
│       │       [🔄 显示解码内容]                      │
│       └─ ...                                         │
│                                                       │
│ 3  ▶ { parentUuid: "6d5b038a...", ...               │
└───────────────────────────────────────────────────────┘
共 1024 行，显示 3 行（过滤后）
```

---

## 五、实施步骤

### Phase 1: 项目搭建（1 天）
- [x] 初始化 Vite + Vue 3 + TypeScript 项目
- [x] 配置 Chrome 扩展开发环境
- [x] 创建基础目录结构
- [x] 设置 ESLint + Prettier

### Phase 2: 核心组件开发（3 天）
- [ ] `JsonLineItem.vue` - 单行 JSON 显示组件
- [ ] `JsonTree.vue` - 递归树形结构组件
- [ ] `StringDecoder.vue` - 字符串解码组件
- [ ] 实现智能解码算法 `decoder.ts`

### Phase 3: 过滤功能（2 天）
- [ ] `SearchFilter.vue` - 搜索过滤 UI
- [ ] `filter.ts` - 实现两种过滤模式
- [ ] 集成解码内容搜索

### Phase 4: Chrome 扩展集成（2 天）
- [ ] Content Script - 拦截网页 JSONL
- [ ] 本地文件上传功能
- [ ] Manifest V3 配置

### Phase 5: UI/UX 优化（1 天）
- [ ] 主题切换
- [ ] 语法高亮
- [ ] 响应式布局
- [ ] 性能优化（虚拟滚动可选）

### Phase 6: 测试与发布（1 天）
- [ ] 功能测试
- [ ] 不同 JSONL 格式兼容性测试
- [ ] 打包并发布到 Chrome Web Store

---

## 六、关键技术难点

### 6.1 递归解码性能
**问题**：深层嵌套的 JSON 字符串递归解析可能导致性能问题

**解决方案**：
- 使用缓存机制，避免重复解析
- 限制最大递归深度（默认 10 层）
- 懒加载：仅在用户展开节点时才解析子节点

### 6.2 大文件处理（未来扩展）
虽然当前不需要，但预留扩展性：
- 使用 `vue-virtual-scroller` 实现虚拟滚动
- 分页加载：每次只加载 100-500 行
- Web Worker 解析大文件

### 6.3 Content Script 注入
**问题**：Content Script 需要访问页面 DOM 但权限受限

**解决方案**：
```typescript
// 使用 iframe 隔离查看器
const iframe = document.createElement('iframe');
iframe.src = chrome.runtime.getURL('viewer.html');
iframe.style.cssText = 'position:fixed; top:0; left:0; width:100%; height:100%; border:none; z-index:999999;';
document.body.innerHTML = '';
document.body.appendChild(iframe);

// 通过 postMessage 传递数据
iframe.onload = () => {
  iframe.contentWindow?.postMessage({
    type: 'LOAD_JSONL',
    data: bodyText
  }, '*');
};
```

---

## 七、关键文件说明

### 核心文件
1. **`manifest.json`** - Chrome 扩展配置文件
2. **`src/viewer/App.vue`** - 主应用入口
3. **`src/utils/decoder.ts`** - 智能解码核心逻辑
4. **`src/utils/filter.ts`** - 过滤算法实现
5. **`src/components/JsonTree.vue`** - 递归树形结构组件

### 配置文件
- `vite.config.ts` - 构建配置（需要配置多入口）
- `tsconfig.json` - TypeScript 配置
- `package.json` - 依赖管理

---

## 八、可选增强功能

1. **导出功能**：导出过滤后的数据为新的 JSONL 文件
2. **历史记录**：记录最近打开的文件
3. **自定义配色**：允许用户自定义语法高亮颜色
4. **快捷键**：支持键盘快捷键（`Ctrl+F` 搜索、`Ctrl+E` 展开全部等）
5. **统计信息**：显示总行数、过滤后行数、JSON 结构统计
6. **Diff 模式**：对比两个 JSONL 文件的差异

---

## 九、预期效果

完成后的扩展将提供：
- ✅ 优雅的 JSON Lines 查看体验
- ✅ 自动识别并美化网页中的 JSONL 响应
- ✅ 智能解码多层嵌套的 JSON 字符串
- ✅ 强大的搜索过滤功能（支持解码后内容搜索）
- ✅ 轻量级（< 500KB）、快速响应
- ✅ 支持亮/暗两种主题

用户可以像使用 JSONView 一样自然地查看 JSONL 数据，无需复制到外部工具进行解码。
